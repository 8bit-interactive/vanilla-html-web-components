{
  "name": "vanilla-html-web-components-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8000 --directory demo",
    "dev": "python3 -m http.server 8000 --directory demo",
    "test": "playwright test"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.2",
    "@playwright/test": "^1.52.0"
  }
}
